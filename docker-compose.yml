version: '3.8'

services:
  backend:
    image: backend:latest
    container_name: backend
    env_file:
      - .env
    networks:
      - internal-network

networks:
  internal-network:
    external: true
